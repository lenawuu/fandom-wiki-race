{
    "scripts": {
        "client": "npm start --prefix ./client",
        "server": "npm start --prefix ./server",
        "all": "npm-run-all --parallel client server"
    },
    "dependencies": {
        "npm-run-all": "^4.1.5"
    }
}
